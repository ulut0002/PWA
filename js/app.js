const APP = {
  currentPage: 'home',
  init() {
    //page has loaded
  },
  addListeners() {
    //add DOM listeners
  },
  navigate(page) {
    //navigate to a new page
  },
};

document.addEventListener('DOMContentLoaded', APP.init);
